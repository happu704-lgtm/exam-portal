[build]
  command = "pip install -r requirements.txt"
  publish = "."

[build.environment]
  PYTHON_VERSION = "3.11"

[[redirects]]
  from = "/*"
  to = "/"
  status = 200

[dev]
  command = "python app.py"
  targetPort = 5000
